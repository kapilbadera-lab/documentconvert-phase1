
import React from 'react';
export default function App(){
  return (<div style={{padding:20,fontFamily:'Arial'}}>
    <h1>SmartDocConvert — The MultiLink Dot Com</h1>
    <p>Phase-1 demo. Upload CSV + photos via backend API at <code>/api/upload</code>.</p>
    <p>Admin: admin@multilink.com / Admin@1234</p>
  </div>);
}
